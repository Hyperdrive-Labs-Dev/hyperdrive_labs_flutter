name: hyperdrive_labs_console_logger
description: "A lightweight, developer-friendly Flutter console logger that transforms package:logging records into structured, ANSI-colored box frames with emoji tags."
version: 0.0.1
homepage: https://www.hyperdrivelabs.dev/
resolution: workspace

environment:
  sdk: ^3.12.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
